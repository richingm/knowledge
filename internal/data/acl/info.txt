acl 是防腐层

定义对外的适配层和feign调用,提供接口给基础设施层的仓储调用


同上下文(同域):
    acl层 -> Adapter适配层 -> 调用其它域的repository   (不得已才使用,不推荐使用)
    推荐: 1.使用领域事件eventbus来做解耦
         2.考虑是否可能合并为一个领域
跨上下文(跨域)：
    acl层->adapter适配器层->feign调用


